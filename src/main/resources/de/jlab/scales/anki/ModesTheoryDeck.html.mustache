<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <title>{{title}}</title>
</head>
<body>
  <div class="container">
    <h1>{{title}}</h1>
    <p>
      This deck contains the scales, modes and chords that are most commonly used in Jazz.  
      For details visit <a href="https://www.ankijazz.com">AnkiJazz</a>
    </p>
    {{#cards}}
      <div class="card mb-3">
        <h5 class="card-header">{{{fields.front}}}</h5>
        <div class="card-body">
          <p class="card-text">{{{fields.back}}}</p>
          <p class="card-text">Fields:</p>
          <ul>
            <li>task: {{fields.task}}</li>
            <li>parentName: {{fields.parentName}}</li>
            <li>parentType: {{fields.parentType}}</li>
            <li>parentRoot: {{fields.parentRoot}}</li>
            <li>modeName: {{fields.modeName}}</li>
            <li>modeType: {{fields.modeType}}</li>
            <li>modeRoot: {{fields.modeRoot}}</li>
          </ul>
        </div>
      </div>
    {{/cards}}
  </div>
</body>
</html>